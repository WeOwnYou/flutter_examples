name: test_project
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  #packages
  sliver_bottom_sheets:
    path: packages/sliver_bottom_sheets
  gps_app:
    path: packages/gps_app
  workers_tree:
    path: packages/workers_tree

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

  #tests dependencies
#  auto_route_generator: ^5.0.2
#  bloc_test: ^9.1.0
#  build_runner: ^2.3.0
flutter:
  uses-material-design: true